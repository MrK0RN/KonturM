# Полный стек (app + PostgreSQL + Mailpit): из корня репозитория — `docker compose up --build`.
# Из каталога api: `docker compose -f compose.yaml up --build` (подключается ../docker-compose.yml).

include:
  - path: ../docker-compose.yml
