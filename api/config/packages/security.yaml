security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'
    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        users_in_memory:
            memory:
                users:
                    admin:
                        password: '%env(string:API_ADMIN_PASSWORD_HASH)%'
                        roles: [ 'ROLE_ADMIN' ]
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        design_ui:
            pattern: ^/design
            security: false
        admin_ui:
            pattern: ^/admin
            security: false
        home_root:
            pattern: ^/$
            security: false
        catalog_redirect_public:
            pattern: ^/catalog$
            security: false
        storefront_pages:
            pattern: ^/(category2|cart|search|product|about|contacts|promo|certificates|price-list)$
            security: false
        price_list_download:
            pattern: ^/price-list\.(pdf|xlsx)$
            security: false
        login:
            pattern: ^/api/auth
            stateless: true
            json_login:
                check_path: /api/auth/login
                success_handler: lexik_jwt_authentication.handler.authentication_success
                failure_handler: lexik_jwt_authentication.handler.authentication_failure
        main:
            lazy: true
            provider: users_in_memory
            stateless: true
            jwt: ~

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        - { path: ^/api/auth/login, roles: PUBLIC_ACCESS }
        - { path: ^/api/orders/[A-Za-z0-9\-]+/status$, roles: PUBLIC_ACCESS }
        - { path: ^/api/orders$, roles: PUBLIC_ACCESS, methods: [POST] }
        - { path: ^/api/cart, roles: PUBLIC_ACCESS }
        - { path: ^/api/search, roles: PUBLIC_ACCESS }
        - { path: ^/api/seo, roles: PUBLIC_ACCESS }
        - { path: ^/api/site-contacts$, roles: PUBLIC_ACCESS, methods: [GET] }
        - { path: ^/api/certificates-catalog$, roles: PUBLIC_ACCESS, methods: [GET] }
        - { path: ^/api/admin, roles: ROLE_ADMIN }
        - { path: ^/api/orders, roles: ROLE_ADMIN }

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
